#!/usr/bin/env bash
set -euo pipefail

bundle check || bundle install
bin/rails db:prepare

if [ "${SEED_DB:-true}" = "true" ]; then
  bin/rails db:seed
fi

exec bin/rails server -b 0.0.0.0 -p "${PORT:-3001}"
